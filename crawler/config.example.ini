# Historical template only. The crawler is unsupported and must not be used to
# collect new data. If inspecting the old login code locally, copy this file to
# config.ini and keep that private file outside version control.
[info]
email =
password =
